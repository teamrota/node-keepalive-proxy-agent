{
  "name": "@teamrota/keepalive-proxy-agent",
  "version": "1.5.3",
  "description": "proxy agent that honors keepalive",
  "main": "index.js",
  "scripts": {
    "bump": "test -n \"$1\" && npm version $1 && npm publish && git push",
    "test": "standard && mocha",
    "lint": "standard index.js testerver.js test/*.js"
  },
  "files": [
    "index.js"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teamrota/node-keepalive-proxy-agent.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "MIT",
  "devDependencies": {
    "pem": "^1.14.6",
    "prettier": "^2.6.2",
    "proxy": "^1.0.2",
    "should": "^13.2.3",
    "si": "^2.0.0"
  },
  "overrides": {
    "glob-parent": "^5.1.2",
    "minimist": "^1.2.6",
    "trim": "^1.0.1",
    "postcss": "^8.4.6"
  }
}
